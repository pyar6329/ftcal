<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">ftcal</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <a href="#" id="nav_username"><%= current_user.name %></a>
          </li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <span id="Menu">Menu</span>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <% if user_signed_in? %>
  	         <%#= current_user.email if user_signed_in? %>
              <li><%= link_to 'サインアウト', destroy_user_session_path, method: :delete, id: "nav_signout" %></li>
            <% else %>
              <li><%= link_to 'サインイン with Google', user_omniauth_authorize_path(:google_oauth2), id: "nav_signin" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
